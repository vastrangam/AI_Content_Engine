# Medhava · Domain 9 — Purchase ✅ **COMPLETE (2 of 2 apps)**

> *Business towards Intelligence*

Both apps in this domain are built, verified and delivered. Each ships in **two formats**, and each format has **three files**: a working tool, the complete build prompt, and an illustrated PDF tour.

## App 1 · Procurement
RFQ → Purchase Order → Goods Receipt → **3-Way Match** → Vendor Scorecard.

| File | What it is |
|---|---|
| `Procurement.html` | The working tool — double-click, works **offline** |
| `Procurement_BUILD_PROMPT.md` | The complete build spec in a code block |
| `Procurement_TOUR.pdf` | 17-page illustrated tour |

**Prove it in 60 seconds:** Backup & Health → **14/14 pass**. 3-Way Match → two bills correctly *held* (*billed 100 > accepted 96*, and *price 150→165*). Wiring → the GRN-501 cascade with ITC ₹1,344.

## App 2 · Vendor Management
Vendor 360 → Bills & Payments → Aging → **Risk** → Performance-Based Sourcing.

| File | What it is |
|---|---|
| `VendorManagement.html` | The working tool — double-click, works **offline** |
| `VendorManagement_BUILD_PROMPT.md` | The complete build spec in a code block |
| `VendorManagement_TOUR.pdf` | 18-page illustrated tour |

**Prove it in 60 seconds:** Backup & Health → **14/14 pass**, then refresh — every figure identical. Aging → ₹64,000 stuck in **60+** (76 days late). Risk → your **best** supplier (99% performance) still flags **medium risk**, purely because 44% of your spend sits with them.

## The two formats

- **FormatA_UnifiedERP_anyIndustry/** — neutral master data. Works unchanged for textile, medical, manufacturing or services; you swap only the vendor/item list.
- **FormatB_Vastrangam/** — your Surat–Jaipur mill base, GST, BUSY ledger, karigar floor.

Both formats run the **identical engine** and pass the **identical 14 tests**. Only the data differs.

## Verification standard

Every app is checked twice before it ships:
1. **Engine tests in Node** — seed + all self-tests, headless.
2. **Real browser** — every screen rendered, every action fired, **zero console errors**.

56/56 self-tests currently pass across the four builds.

## Honest scope

Local-first, single browser. The hosted multi-tenant edition runs the same engines against Postgres with row-level security and an event bus, where cross-module posting is real rather than illustrated. Real integrations use **revocable, scoped API keys in an encrypted vault — never account passwords.**

**Next:** Domain 7 · Inventory, then Domain 5 · Warehouse.
